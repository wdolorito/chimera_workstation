#!/bin/sh

#
# ~/local/bin/sys_update
#

CMD="apk"
SUDO="$(which sudo 2>/dev/null)"

if [ -n "$SUDO" ]
then
	$SUDO "$CMD" upgrade --available --prune --no-interactive
else
	echo "sudo command not available."
	exit 127
fi

flatpak_update
